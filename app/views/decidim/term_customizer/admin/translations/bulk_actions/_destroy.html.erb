<div id="js-destroy-translations-actions" class="hidden js-bulk-action-form">
  <%= form_tag(translation_set_translations_destroy_path(set), method: :delete, id: "js-form-destroy-translations", class: "flex items-center justify-center gap-1") do %>
    <div class="checkboxes hidden">
      <% translations.each do |translation| %>
        <%= check_box_tag "translation_ids[]", translation.id, false, class: "js-check-all-translation js-translation-id-#{translation.id}" %>
      <% end %>
    </div>

    <%= submit_tag(t("actions.destroy", scope: "decidim.admin"), id: "js-submit-destroy-translations", class: "button button__sm button__secondary") %>

    <button id="js-cancel-destroy-translations" class="button button__xs button__text-secondary js-cancel-bulk-action" type="button"><%= t("decidim.term_customizer.admin.actions.cancel") %></button>
  <% end %>
</div>
